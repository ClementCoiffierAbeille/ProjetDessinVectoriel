#include "Form.h"
#include <iostream>

// Constructeur de la classe Form
Form::Form(int x, int y, const std::string& color, int transparency)
    : x(x), y(y), color(color), transparency(transparency) {}

// Destructeur de la classe Form
Form::~Form() {


}